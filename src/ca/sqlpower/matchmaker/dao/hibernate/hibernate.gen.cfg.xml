<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
		"-//Hibernate/Hibernate Configuration DTD 3.0//EN"
		"http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">
<!-- This is the config file that we used to generate the mapping
     files in the first place.  We've pretty much re-done them by
     hand since then, though, so the tools that this config applies to
     are probably useless to us now.
  -->
<hibernate-configuration>

     <session-factory>
        <property name="connection.driver_class">oracle.jdbc.driver.OracleDriver</property>
        <property name="connection.password">newpl</property>
        <property name="connection.url">jdbc:oracle:thin:@arthur:1521:TEST</property>
        <property name="connection.username">newpl</property>
        <property name="default_schema">NEWPL</property>
        <property name="dialect">org.hibernate.dialect.OracleDialect</property>
   <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">1</property>
        <!-- Enable Hibernate's automatic session context management -->
        <property name="current_session_context_class">thread</property>
        <!-- Disable the second-level cache  -->
        <property name="cache.provider_class">org.hibernate.cache.NoCacheProvider</property>
        <property name="show_sql">true</property>
     
    </session-factory>

</hibernate-configuration>
